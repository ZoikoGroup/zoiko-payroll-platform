# When empty or unset, the API client automatically resolves the backend URL
# from the current page hostname:
#   http://localhost:5173  → http://localhost:8000
#   http://192.168.31.148:5173  → http://192.168.31.148:8000
# Set explicitly only when the backend runs on a different host/port.
VITE_API_BASE_URL=
