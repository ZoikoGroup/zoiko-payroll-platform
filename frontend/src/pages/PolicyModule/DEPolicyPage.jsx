import PolicyLayout from "./shared/PolicyLayout";

// Germany's Policy authoring page — see INPolicyPage.jsx's comment for the
// shared-vs-jurisdiction-specific split this file is part of.
export default function DEPolicyPage() {
  return <PolicyLayout country="DE" countryName="Germany" />;
}
