import PolicyLayout from "./shared/PolicyLayout";

// Canada's Policy authoring page — see INPolicyPage.jsx's comment for the
// shared-vs-jurisdiction-specific split this file is part of.
export default function CAPolicyPage() {
  return <PolicyLayout country="CA" countryName="Canada" />;
}
