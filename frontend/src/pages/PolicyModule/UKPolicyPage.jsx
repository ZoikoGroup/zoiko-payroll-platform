import PolicyLayout from "./shared/PolicyLayout";

// United Kingdom's Policy authoring page — see INPolicyPage.jsx's comment
// for the shared-vs-jurisdiction-specific split this file is part of.
export default function UKPolicyPage() {
  return <PolicyLayout country="UK" countryName="United Kingdom" />;
}
